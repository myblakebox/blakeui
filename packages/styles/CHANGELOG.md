# @blakeui/styles

## 1.1.0

### Minor Changes

- Button now ships the fancy gradient layers (gradient inner border + top-down sheen) and `shadow-surface` elevation by default on the filled variants (primary, secondary, tertiary, danger, danger-soft). The `ghost` and `outline` variants opt out of both and stay flat. ButtonGroup carries a single group-level shadow while its segments render flat (no per-segment shadow or gradient frame), keeping the sheen on each segment.

## 3.0.1

### Patch Changes

- README updated

## 3.0.0

### Patch Changes

- v3.0.0
