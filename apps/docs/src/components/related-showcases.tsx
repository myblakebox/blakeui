"use client";

interface RelatedShowcasesProps {
  component: string;
  className?: string;
}

// Temporarily hidden everywhere. The previous implementation (rendering a grid of
// related showcases) is available in git history — restore it to bring the block back.
export function RelatedShowcases(_props: RelatedShowcasesProps) {
  return null;
}
